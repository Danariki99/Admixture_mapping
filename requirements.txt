matplotlib==3.7.5
numpy==1.20.3
pandas==1.3.5
PyYAML==5.1.2
scikit-learn==1.0.1
scipy==1.10.1
seaborn==0.11.2
sklearn-crfsuite==0.3.6
tqdm==4.62.3
uncertainty-calibration==0.1.4
xgboost==1.1.1
requests==2.32.3
Pillow==9.5.0
snputils==0.2.11
# --- NEW pipeline dependencies (main environment) ---
statsmodels==0.13.5        # BY (fdr_by) correction: admixture-mapping post-processing + conditional fine mapping
pyliftover==0.4            # hg19 -> hg38 liftover for the Borzoi input VCF
h5py==3.7.0                # read Borzoi SED .h5 output (post-processing)
polars==0.20.31            # fast covariate-file handling (new_covariable_creation)
openpyxl==3.1.2            # .xlsx tables

# =====================================================================
# Borzoi runs in a SEPARATE environment (TensorFlow, incompatible with the
# numpy 1.20 pinned above). Install these in that env, NOT here:
#   tensorflow==2.15.*     # (+ nvidia-*-cu12 CUDA libs for GPU)
#   baskerville            # from the Borzoi repo
#   borzoi                 # from the Borzoi repo (src/)
#   pybedtools             # needs the `bedtools` binary on PATH
#   intervaltree
#   pysam
#   pyfaidx
# =====================================================================
